<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ feed_url }}</id>
  <title>{{ site.title }}</title>
  <subtitle>{{ site.description }}</subtitle>
  <updated>{{ feed_updated }}</updated>
  <link rel="alternate" type="text/html" href="{{ site_url }}"/>
  <link rel="self" type="application/atom+xml" href="{{ feed_url }}"/>
  {% for project in projects %}
  <entry>
    <id>{{ project.repo_url if project.type == 'repo' else project.gist_url }}</id>
    <title>{{ project.name }}</title>
    <updated>{{ project.feed_updated }}</updated>
    <published>{{ project.created_at | atom_date }}</published>
    <link rel="alternate" type="text/html" href="{{ project.repo_url if project.type == 'repo' else project.gist_url }}"/>
    {% if project.description %}<summary>{{ project.description }}</summary>{% endif %}
    {% for tag in project.tags %}<category term="{{ tag }}"/>
    {% endfor %}
  </entry>
  {% endfor %}
</feed>
